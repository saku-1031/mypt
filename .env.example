# OpenAI API Configuration (for TTS)
OPENAI_API_KEY=sk-proj-...
OPENAI_BASE_URL=https://api.openai.com/v1

# Google Cloud Speech-to-Text Configuration (for ASR)
# Option 1: Use service account key file (recommended for local development)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Option 2: For production, use workload identity or instance metadata
# (No env var needed - automatic authentication)

# Google Cloud Project ID (optional, usually auto-detected)
GOOGLE_CLOUD_PROJECT=your-project-id
